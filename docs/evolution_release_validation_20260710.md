# 强势回调策略自进化发布验证

## 验证范围

- 代码提交：`31f17b587b1f7cd926dc3601762c520040ba8b04`
- 数据截至：`2026-07-10`
- 股票面板：主板与创业板历史面板，约 295.7 MB
- 基准：`510300`，截至 `2026-07-10`
- 模式：`dry-run`，不改正式状态、不改策略 YAML、不连接券商

## 自动化验证

- 完整测试：`251 passed, 498 subtests passed`
- Python 编译检查通过
- `git diff --check` 通过
- 工作区无未提交变更

## 两次完整回放

两次回放使用相同数据、配置、代码和 `run_id`，分别写入独立输出目录。

| 项目 | 首次 | 复验 |
| --- | ---: | ---: |
| 状态 | success | success |
| 研究冠军 | risk_075 | risk_075 |
| 最终状态 | not_opened_core_rejected | not_opened_core_rejected |
| 耗时 | 3983.41 秒 | 3546.12 秒 |
| 峰值内存 | 2,094,444,544 字节 | 2,157,940,736 字节 |
| 输出文件数 | 67 | 67 |

两次输出中 65 个文件字节级完全一致。仅以下运行元数据不同：

- `manifest.json` 的创建/完成时间、耗时、峰值内存和输出路径。
- `evolution_state_snapshot.json` 的 `updated_at`。

候选评分、实验编号、折叠指标、交易审计、轮次决定、冠军配置和拒绝原因均一致。

## 研究结论

`risk_075` 在选择期通过旧版与通用门禁：

- 选择期总收益：`19.14%`
- 选择期年化收益：`45.82%`
- 选择期最大回撤：`-9.14%`
- 三个选择折均为正

锁定候选进入独立核心测试后被拒绝：

- 相对基线为正的核心折占比：`1/3`，低于 `2/3` 门槛
- 核心折平均收益提升：`0.57` 个百分点，低于 `1` 个百分点门槛
- 最终留出段按设计未开启，避免用留出结果反向选择参数

因此当前证据支持保留现有正式版本，不自动晋级 `risk_075`。该结论表示收益提升尚不稳定，不表示该候选在所有区间都无效。

## 安全审计

- `global_state_changed=false`
- 正式状态文件、转换日志和锁文件均未生成
- 策略配置 SHA-256 保持为 `708679C0C0B3DBB9791B57BF108CC1FC38D55C8A27F8A88CDB7E47823AFB82D3`
- 未产生券商订单，也未修改每日生产流程
